nav#main-nav(class='navbar navbar-expand-lg navbar-dark bg-dark sticky-top navbar-light bg-light')
    div(class='container-fluid')
        if isAuth
            a(class="navbar-brand text-bold", href="/")
                strong ESN 
                span#user-loggedin(style="text-transform: capitalize; color: #a1ccd6;")
        else
            a(class="navbar-brand text-bold", href="/home")
                strong ESN  
                span#user-loggedin(style="text-transform: capitalize;color: #a1ccd6;")
        button(class="navbar-toggler", type="button", data-bs-toggle="collapse", data-bs-target="#navbarTogglerDemo02", aria-controls="navbarTogglerDemo02", aria-expanded="false", aria-label="Toggle navigation")
            span(class="navbar-toggler-icon")
        div#navbarTogglerDemo02(class="collapse navbar-collapse")
            ul(class="navbar-nav ms-auto mb-2 mb-lg-0")
                if !(isAuth)
                    li(class="nav-item" id="activate-company" style="display: none;")
                        a(class="nav-link", aria-current="page", href="/companyList" style="color: white; font-weight: 500;") Inactive Company
                    li(class="nav-item")
                        a(class="nav-link", aria-current="page", href="/approvedCompany" style="color: white; font-weight: 500;") Health-service
                    li(class="nav-item")
                        a(class="nav-link", aria-current="page", href="/share-status" style="color: white; font-weight: 500;") Status
                    li(class="nav-item")
                        a(class="nav-link", aria-current="page", href="/view-directory" style="color: white; font-weight: 500;") Directory
                    li(class="nav-item")
                        a(class="nav-link", aria-current="page", href="/chat-publicly" style="color: white; font-weight: 500;") Public
                    li(class="nav-item")
                        a(class="nav-link", aria-current="page", href="/postAnnouncement" style="color: white; font-weight: 500;") Announcement
                    li(class="nav-item")
                        a(class="nav-link", aria-current="page", href="/read-and-support" style="color: white; font-weight: 500;") Read
                        a(class="nav-link active", aria-current="page", href="#", class="logout-btn", style="color: white; font-weight: 500;") Logout
                if(isHealth)
                    li(class="nav-item" id="logout-btn")
                        a(class="nav-link active", aria-current="page", href="#", id="logout-btn-company", style="color: white; font-weight: 500;") Logout
        script(src='/js/logout.js' type='text/javascript')
        script(src='/js/loggedInUser.js' type='text/javascript')
        script(src='/js/isAdmin.js' type='text/javascript')
        script(src='/js/logoutComp.js' type='text/javascript')
